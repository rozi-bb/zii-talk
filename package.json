{
  "name": "zii-talk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n api,web -c magenta,cyan \"PYTHONUNBUFFERED=1 uv run python -m server.main --reload\" \"vite\"",
    "build": "tsc --noEmit && vite build",
    "start": "PYTHONUNBUFFERED=1 uv run python -m server.main",
    "serve": "npm run build && npm run start",
    "studio": "uv run --group dev langgraph dev",
    "studio:tailnet": "BRIDGE_TARGET=2024 node scripts/bridge.mjs on && uv run --group dev langgraph dev --host 0.0.0.0 --port 2024 --no-browser",
    "bridge": "node scripts/bridge.mjs on",
    "bridge:off": "node scripts/bridge.mjs off",
    "bridge:status": "node scripts/bridge.mjs status",
    "tailnet": "npm run build && node scripts/bridge.mjs on && npm run start",
    "dev:tailnet": "BRIDGE_TARGET=5173 node scripts/bridge.mjs on && ZII_TUNNEL=1 npm run dev"
  },
  "dependencies": {
    "microsoft-cognitiveservices-speech-sdk": "^1.48.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.1.0",
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3",
    "vite": "^7.1.0"
  }
}
